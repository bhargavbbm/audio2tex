# ── Build tools & dependencies ──────────────────────────────────────────────
setuptools>=65.5.0
wheel>=0.38.0

# ── Web framework ──────────────────────────────────────────────────────────
fastapi
uvicorn[standard]==0.29.0
python-multipart

# ── Whisper (OpenAI) ────────────────────────────────────────────────────────
openai-whisper

# ── PyTorch — CPU-only build (much smaller, fine for HF free tier) ─────────
# HF Spaces usually pre-installs torch; this pin avoids re-downloading.
# If your Space has GPU, remove the --index-url line and just keep torch.
torch
torchaudio
